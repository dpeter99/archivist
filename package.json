{
  "name": "archivist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "run": "deno run --allow-read --unstable --allow-write .\\index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "marked": "^3.0.2"
  }
}
